apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: hello-whale
spec:
  components:
    - name: hello-whale
      type: webservice
      properties:
        image: crccheck/hello-world
        port: 8000
      traits:
        - type: gateway
          properties:
            domain: localhost
            http:
              "/": 8000